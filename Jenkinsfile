pipeline {

    agent {
        label "master"
    }


    stages {

        stage("init") {
                      
            steps {
                echo 'testing the master branch'
            }
        }

        stage("build") {
                      
            steps {
                echo 'testing the master branch'
            }
        }
               
        stage("test") {
                         }
            echo 'testing the master branch'
            }
        }

        stage("deploy"){

            steps{
              echo 'testing the master branch'

            }
        }
    }
}
